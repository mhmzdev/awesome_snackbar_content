name: awesome_snackbar_content
description: Uplift your snackbar experience
version: 0.0.2
homepage: https://github.com/mhmzdev/awesome_snackbar_content

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter_svg: ^1.0.3

  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# The following section is specific to Flutter packages.
flutter:

  assets:
    - assets/
    - assets/types/